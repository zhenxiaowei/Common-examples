

  //  Ajax请求默认的都是异步的  如果想同步 async设置为false就可以（默认是true）

  responseText：你向ajax后台的程序发送xmlhttp请求的时候, 后台程序接到请求会进行处理,处理结束后,可以返回一串数据给前台,这个就是responseText. 一般在后台程序C#中是Response.Write("字符串") php中使用的是echo...就是一个输出字符串而已.

  JSON.parse(data) //由JSON字符串转换为JSON对象

  //hasOwnProperty：是用来判断一个对象是否有你给出名称的属性或对象。注意:此方法无法检查该对象的原型链中是否具有该属性，该属性必须是对象本身的一个成员。

  //indexOf(字符)：获取指定字符在字符串中第一次出现的索引位置，//如果没有这个字符返回的是-1，基于这个理念我们经常用这两个方法判断字符串中是否包含某个字符

  //Math.random()获取随机数

